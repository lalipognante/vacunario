import { FiltroRecordatoriosPipe } from './filtro-recordatorios.pipe';


describe('FiltroRecordatoriosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroRecordatoriosPipe();
    expect(pipe).toBeTruthy();
  });
});
